##defines the domain specific behavior for the AI Governance domain

domain: ai_governance

display_name: AI Governance

description: AI Risk Management and Governance Knowledge Base

metadata_fields:
  - document_version
  - publication_date
  - document_type
  - section
  - category
  - source

retrieval:
  top_k: 10

evaluation:
  enabled: true

recommendations:
  enabled: true

versioning:
  enabled: true

change_detection:
  enabled: true